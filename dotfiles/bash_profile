export PATH="$PATH:~/bin"
export PATH="$PATH:~/.cargo/bin"
cdl() {
  if [ ! -z "$@" ]
  then
    DIR="$@"
  else
    DIR="`pwd`"
  fi
  cd "$DIR" && pwd && ls -la
}
if which rbenv > /dev/null; then eval "$(rbenv init -)"; fi
stty -ixon
if [ -f ~/.bashrc ]; then
   source ~/.bashrc
fi
